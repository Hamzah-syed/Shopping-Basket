import React from "react";

//components
import CartProducts from "../components/cart/cartProducts";

const Cart = () => {
  return (
    <div>
      <CartProducts />
    </div>
  );
};

export default Cart;
